---
interface Props {
  gap?: string;
}

const { gap = '2rem' } = Astro.props;
---

<div class="columns-1 md:columns-2" style={`column-gap: ${gap}; column-fill: balance;`}>
  <slot />
</div>

<style>
  div {
    column-fill: balance;
  }
</style>
